[project]
name = "human-presentator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "zonos",
]

[tool.uv.sources]
zonos = { git = "https://github.com/mitsaucepls/Zonos.git" }
